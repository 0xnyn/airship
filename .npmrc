# Allow build scripts for known-good native/postinstall deps (pnpm 10+ blocks by default)
auto-install-peers=true
strict-peer-dependencies=false
# Hoist common dev tooling so editor/tsserver resolves them at the root
public-hoist-pattern[]=*biome*
public-hoist-pattern[]=*types*
